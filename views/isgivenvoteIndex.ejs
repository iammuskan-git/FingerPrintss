<%- include('./includes/header') %>

<div class="container">
    <a href="/addIsGivenVote">
        <button class="float-right btn btn-primary" >Create</button>
    </a>
    <div class="row my-4">
        <div class="col-lg-12">
            <% if(message) { %>
                <div class="alert alert-dismissible fade show alert-<%= message.type %>" role="alert">
                    <button class="btn-close" type="button" data-bs-dismiss="alert" aria-label="Close"></button>
                    <strong><%= message.message %></strong>
                </div>
                <% } %>
           <div class="table-responsive">
            <% if(users != '') { %>
                <table class="table table-striped">
                    <thead>
                        <tr class="table-dark">
                            <th class="text-center">ID</th>
                            <th class="text-center">Voter_ID</th>
                            <th class="text-center">Action</th>
                        </tr>
                    </thead>
                    <tbody>
                      <%  users.forEach((row) => { %> 
                        <tr class="align-middle">
                            <td><%= row.id%></td>
                            <td><%= row.voter_id%></td>
                            <td>
                                <a href="/editss/<%= row.id %>" class="text-success"><i class="fas fa-edit fa-lg mx-1"></i></a>
                                <a href="/deletess/<%= row.id %>" class="text-danger"><i class="fas fa-trash fa-lg mx-1"></i></a>
                            </td>
                        </tr>
                        <% }) %> 
                    </tbody>
                </table>
                <% } else { %>
                    <h1 class="text-center text-secondary mt-5"> No IsGivenVote found in the database!</h1>
            <% } %>
           </div>
        </div>
    </div>
</div>
<%- include('./includes/footer') %>